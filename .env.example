# Slack (Socket Mode)
SLACK_BOT_TOKEN=xoxb-***             # Bot User OAuth Token
SLACK_APP_TOKEN=xapp-***             # App-level token (connections:write)
SLACK_SIGNING_SECRET=***             # For request verification (Bolt handles it) 
SLACK_CHANNEL_ORCH=#orchestrator

# Providers
OPENAI_API_KEY=***
ANTHROPIC_API_KEY=***

# Budget
WEEKLY_USD_CAP=75
SOFT_ALERT_PCT=80
BOOST_STOP_LOSS=5

# Cursor
CURSOR_CLI=cursor        # if different, set full path

# Orchestrator defaults
# Absolute path to repo root (override auto-detect if needed)
REPO_ROOT=
# Default model for agent runs (used by Slack /agent and /orchestrator run)
CURSOR_AGENT_MODEL=gpt-5
