# Text normalization
* text=auto

# Linux shell scripts must be LF (WSL/CI)
*.sh   text eol=lf
*.bash text eol=lf

# PowerShell stays CRLF on Windows
*.ps1  text eol=crlf
*.psm1 text eol=crlf

# Workflows & YAML prefer LF (CI/Linux)
*.yml  text eol=lf
*.yaml text eol=lf

# Common text defaults to LF (less churn in CI)
*.md   text eol=lf
*.json text eol=lf
*.py   text eol=lf
*.txt  text eol=lf
